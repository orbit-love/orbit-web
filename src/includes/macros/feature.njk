{% macro feature(emoji, title, content) %}
<span class="mr-2">{{ emoji }}</span>
<div>
  <h2 class="text-xl font-bold mb-1">{{ title }}</h2>
  <p class="text-gray-600 leading-snug">
    {{ content }}
  </p>
</div>
{% endmacro %}
